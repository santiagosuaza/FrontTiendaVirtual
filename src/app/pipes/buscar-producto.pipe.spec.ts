import { BuscarProductoPipe } from './buscar-producto.pipe';

describe('BuscarProductoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarProductoPipe();
    expect(pipe).toBeTruthy();
  });
});
